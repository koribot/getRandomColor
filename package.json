{
  "type": "module",
  "name": "get-random-colors",
  "keywords": [
    "random colors",
    "RGB, HSL, HEX color generator",
    "randomize colors",
    "get random colors"
  ],
  "version": "1.0.6",
  "description": "Provide a list of random colors in Hex, RGB, and HSL with adjustable intensity(1-100) as Parameters",
  "main": "./src/lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./src/lib/index.js"
  },
  "author": "walid",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {}
}
